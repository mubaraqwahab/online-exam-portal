<?php

$examID = $_POST['examID'];
$assigneeID = $_POST['assigneeID'];

// Select type id, no of questions from exam id
// For each question,
  // Get score
  // If its fill in,
    // Send to fill in table
  // If its theory,
    // Send to theory table

// Redirect to feedback page with success or failure param
?>